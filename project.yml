# AI Session Orchestrator — Project Configuration
# Auto-generated by ``orchestrator init --name vietsale-pro-v7``

project:
  name: vietsale-pro-v7
  version: "0.1.0"

driver:
  name: cline
  timeout_seconds: 300

workflow:
  max_concurrent: 1
  default_max_retries: 3

storage:
  backend: json
  path: .orchestrator

retry:
  max_attempts: 3
  base_delay_seconds: 5
  max_delay_seconds: 300
  backoff_multiplier: 2.0

context:
  max_tokens: 128000
  compression_threshold: 0.8

queue:
  autosave_interval_seconds: 30

dashboard:
  refresh_interval_seconds: 2
  enabled: true
