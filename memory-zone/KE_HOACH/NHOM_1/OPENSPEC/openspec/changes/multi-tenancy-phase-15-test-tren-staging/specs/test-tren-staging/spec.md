## ADDED Requirements

### Requirement: Test trên staging (giữ nguyên)

The system MUST chạy đầy đủ trên staging trước khi deploy production.

#### Scenario: Sub-phase 15 succeeds
- **GIVEN** the sub-phase is implemented
- **WHEN** the verification steps are executed
- **THEN** all acceptance criteria pass

## MODIFIED Requirements

- None outside this sub-phase.

## REMOVED Requirements

- None outside this sub-phase.
