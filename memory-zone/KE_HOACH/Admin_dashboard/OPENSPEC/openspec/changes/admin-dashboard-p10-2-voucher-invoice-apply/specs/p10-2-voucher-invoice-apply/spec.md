## ADDED Requirements

### Requirement: p10-2-voucher-invoice-apply
The system MUST implement p10.2 — rpc apply voucher/promotion to invoice with limits and conditions..

#### Scenario: p10-2-voucher-invoice-apply happy path
- **GIVEN** the system admin is authenticated and the prerequisite phases are complete
- **WHEN** the system admin performs the P10 2 Voucher Invoice Apply actions
- **THEN** the system applies the changes correctly and returns the expected data

## MODIFIED Requirements

<!-- Only if existing capabilities are changing. -->

## REMOVED Requirements

<!-- Only if deprecating features. -->
