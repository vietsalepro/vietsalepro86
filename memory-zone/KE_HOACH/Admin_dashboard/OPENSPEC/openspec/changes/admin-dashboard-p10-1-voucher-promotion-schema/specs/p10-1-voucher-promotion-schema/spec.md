## ADDED Requirements

### Requirement: p10-1-voucher-promotion-schema
The system MUST implement p10.1 — voucher/promotion schema + backend crud: promo_codes, promotion_rules, usage tracking..

#### Scenario: p10-1-voucher-promotion-schema happy path
- **GIVEN** the system admin is authenticated and the prerequisite phases are complete
- **WHEN** the system admin performs the P10 1 Voucher Promotion Schema actions
- **THEN** the system applies the changes correctly and returns the expected data

## MODIFIED Requirements

<!-- Only if existing capabilities are changing. -->

## REMOVED Requirements

<!-- Only if deprecating features. -->
